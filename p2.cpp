#include<iostream>

#define SIZE 4000000

using namespace std;

int main(int argc, char const *argv[]) {
  int x1,x2,option;
  x1 = 1;
  x2 = 2;
  option = 0;
  while (x1<SIZE) || (x2<SIZE) {
    if(option == 0){
      if (x2 ==) {
        /* code */
      }
      x1 = x1 + x2;
    }
    if (option == 1) {
      x2 = x1 + x2;
    }
  }
  return 0;
}
